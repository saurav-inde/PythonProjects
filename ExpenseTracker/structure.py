"""

interface:
    # add the expense with options -f --food, -c --clothes, -e --electronics
    # default option is food if not specified

    # views the expense for the -n days
    # views the total spent in each category
    # views the total of all category


    # delete the database for months before last two months
    # option -d n
    # n is number of months before which data is deleted

database:
    # fields : AUTO INCREMENT PRIMARY KEY, category, amount, date(AUTO), time(AUTO)
    # fields to be input: category, amount








"""